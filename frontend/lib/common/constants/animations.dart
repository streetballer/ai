import 'package:flutter/material.dart';

const Duration animationSlow = Duration(milliseconds: 800);
const Duration animationMedium = Duration(milliseconds: 400);
const Duration animationFast = Duration(milliseconds: 200);

const Curve animationCurve = Curves.easeOut;
